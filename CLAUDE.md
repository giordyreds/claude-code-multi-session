# claude-code-multi-session

## Agent skills

### Issue tracker

Issues and PRDs live as GitHub issues on `giordyreds/claude-code-multi-session`,
via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, each label string equal to its role name.
See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — `CONTEXT.md` and `docs/adr/` at the repo root.
See `docs/agents/domain.md`.
