{
  "name": "claude-code-multi-session",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "bin": {
    "ccp": "dist/bin.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit false && chmod +x dist/bin.js",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.9.3",
    "vitest": "^2.0.5"
  }
}
